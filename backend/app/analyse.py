"""
    Handles uploaded car images, sends them to the AI (via LangChain + Ollama),
    and returns a structured response with estimated price and condition analysis.
    Images are processed in-memory without being saved.
"""
